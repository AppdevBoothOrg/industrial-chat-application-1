<h1>New Comment</h1>

<%= turbo_frame_tag "new_comment", target: "_top" do %>
  <%= render '/comments/form', comment: Comment.new(conversation_id: @conversation.id) %>
<% end %>

<%= link_to 'Back', comments_path %>
