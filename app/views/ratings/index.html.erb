<h1> Ratings </h1>

<h3> 1- Ratings given by <%= current_user.username.capitalize() %>: </h3>

<tr>
  <% current_user.gradee_ratings.each do |a_rating| %>
    <li>
      <%= "Rating of #{a_rating.grade} given to #{a_rating.grader.username.capitalize() } " %>
    </li>
  <% end %>
<tr>